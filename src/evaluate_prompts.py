"""
Phase 4 (companion) — Prompt evaluation harness.

Runs the classifier over the labelled notes from Phase 1 (ground_truth.csv),
computes accuracy, surfaces the failures, and lets you compare two prompt
versions — this is the JD's "prompt testing, evaluation, optimization".

Output: a printed accuracy number and a per-version comparison you can screenshot.

Status: TODO — implemented in Phase 4.
"""

from __future__ import annotations

import config  # noqa: F401


def main() -> None:
    raise NotImplementedError("Phase 4 — run the Phase 4 prompt to build this.")


if __name__ == "__main__":
    main()
